{
  "name": "mazganim2",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "node scripts/build.mjs",
    "build:checked": "npm run check:launch && npm run build && npm run check",
    "preview": "vite preview --host 127.0.0.1",
    "check": "node --test tests/site.test.mjs",
    "check:launch": "node scripts/check-launch.mjs",
    "dev:worker": "wrangler dev",
    "deploy": "npm run build:checked && wrangler deploy"
  },
  "dependencies": {
    "vue": "^3.5.42"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^6.0.8",
    "@vue/server-renderer": "^3.5.42",
    "vite": "^8.3.0",
    "wrangler": "^4.131.0"
  }
}
